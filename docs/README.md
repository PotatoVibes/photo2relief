# docs/

Assets referenced by the top-level `README.md`.

- **`screenshot.png`** — hero screenshot of the app UI shown at the top of the main README.
  Recommended: the full window with a photo loaded and both previews (2D hillshade + 3D)
  rendered, ~1600 px wide. Note this image is committed to the repo, so pick a photo you're
  happy to publish (the help-page example renders under `static/help/img/` are gitignored for
  privacy — you may want a neutral demo image here too).
